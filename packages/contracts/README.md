# `@coffeebar/contracts`

Shared Zod schemas and TypeScript types for CoffeeBar Web, API, and mobile clients.

This package is intentionally source-first: its export points to raw TypeScript and is intended for modern bundler-based consumers such as Next.js and Vite. It does not provide a precompiled Node.js distribution.
